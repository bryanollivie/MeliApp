package com.bryanollivie.appml.data

import com.bryanollivie.appml.data.remote.Response
import com.bryanollivie.appml.domain.MeliRepository
import javax.inject.Inject
/*

class MeliRepositoryImpl @Inject constructor(
    private val api : ApiService
) : MeliRepository{

    override suspend fun getSearchProd(prod: String): Response {
        return api.getSearchProd(prod=prod)
    }
}
*/
